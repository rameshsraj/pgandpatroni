Phase 3 preparation audit (2026-09-08; before instrumented benchmark runs)
=======================================================================
These entries are reconstructed from the session, not timestamped benchmark
measurements. Per-command start/end times begin in each run's commands.jsonl.

Read-only environment commands:
  docker context show
    desktop-linux
  docker compose ps -a
    etcd, haproxy, pg-node-1..4 healthy; no pg-node-5 container yet.
  docker exec pg-node-1 patronictl -c /etc/patroni/patroni.yml list -f json
    pg-node-3 Leader, nodes 1/2/4 streaming replicas, TL=3, reported lag=0 MB.
  docker exec pg-node-1 pgbench --version
    pgbench (PostgreSQL) 16.4 (Debian 16.4-1.pgdg120+2)
  docker info --format 'CPUs={{.NCPU}} Memory={{.MemTotal}}'
    CPUs=8 Memory=8210579456
  Get-Command pwsh, node, cargo -ErrorAction SilentlyContinue | Select-Object Name,Source
    PowerShell and Node found; cargo not returned. No new application stack installed.
  git status --short
    Existing Phase 2 compose/config/script modifications and untracked evidence preserved.

Preflight syntax commands:
  [System.Management.Automation.Language.Parser]::ParseFile(<scripts/phase3/run-lab.ps1>, ...)
    No PowerShell parser errors.
  docker compose --profile elastic config --quiet
    No Compose validation output.
  docker exec haproxy haproxy -c -f /usr/local/etc/haproxy/haproxy.cfg
    FAILED: HAProxy 2.9.15 does not support server keyword init-state.
    Corrective code change: remove init-state; keep role/lag health checks and DNS
    resolvers. Invalid configuration was NOT restarted/deployed.
  docker exec haproxy sh -c 'command -v nc'
    /usr/bin/nc
  docker run --rm --entrypoint /bin/bash --mount 'type=bind,source=C:\GitHub\pgandpatroni\postgres-patroni-ha\scripts\phase3,target=/work,readonly' postgres-patroni-ha-pg-node-1 -n /work/traffic.sh
    No Bash syntax errors.

Changes intentionally deployed by instrumented run before measurement:
  - Add optional pg-node-5 Compose profile and data volume (never delete volume).
  - Add predeclared pg-node-5 HAProxy backend, Docker DNS resolvers, explicit
    round-robin read routing, 1 MiB replica lag health gate, local runtime API.
  - Render optional nofailover tag for elastic replica; suppress credential dump.
  - Create isolated phase3_lab schema with 100000 immutable fixture rows.
  - One HAProxy restart loads the feature configuration BEFORE the baseline.

Policy is a LAB demonstration based on observed successful read TPS (>30 for
two samples to scale out, <10 for two samples to drain/scale in), not a proven
production saturation or latency policy. Original four nodes are the minimum.